const config = {
  // 启动的端口
  port: 3000,
  // 数据库配置
  database: {
    DATABASE: 'nodesql',
    USERNAME: 'root',
    PASSWORD: 'liqing',
    PORT: '3306',
    HOST: 'localhost'
  }
}

module.exports = config